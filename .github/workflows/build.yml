name: Build PySide6 Android APK

on:
  push:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-22.04

    steps:
    - name: Checkout Code
      uses: actions/checkout@v4

    - name: Install System Dependencies
      run: |
        sudo apt-get update
        sudo apt-get install -y git zip unzip openjdk-17-jdk python3-pip autoconf libtool pkg-config zlib1g-dev cmake libffi-dev libssl-dev

    - name: Setup Java (JDK 17)
      uses: actions/setup-java@v4
      with:
        distribution: 'temurin'
        java-version: '17'

    - name: Setup Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.11'

    - name: Install Android NDK & SDK Fix
      run: |
        echo "y" | $ANDROID_HOME/cmdline-tools/latest/bin/sdkmanager --install "ndk;25.2.9519653"
        echo "ANDROID_NDK_ROOT=$ANDROID_HOME/ndk/25.2.9519653" >> $GITHUB_ENV
        mkdir -p $ANDROID_HOME/tools/bin
        ln -s $ANDROID_HOME/cmdline-tools/latest/bin/sdkmanager $ANDROID_HOME/tools/bin/sdkmanager
        # Accept Licenses (Crucial to prevent Gradle Fail)
        echo "y" | $ANDROID_HOME/cmdline-tools/latest/bin/sdkmanager --licenses

    - name: Install PySide6 Tools
      run: |
        pip install PySide6==6.8.0 buildozer cython requests

    - name: Safe Surgery PySide6 Wheel
      run: |
        wget -q https://download.qt.io/official_releases/QtForPython/pyside6/PySide6-6.8.0-6.8.0-cp311-cp311-android_aarch64.whl
        unzip -q PySide6-6.8.0-6.8.0-cp311-cp311-android_aarch64.whl -d ps6_temp
        
        # 1. DELETE NON-BINARY CLUTTER (100% Safe to delete)
        rm -rf ps6_temp/PySide6/Qt/translations
        rm -rf ps6_temp/PySide6/Qt/doc
        rm -rf ps6_temp/PySide6/Qt/examples
        
        # 2. DELETE ONLY THE LARGEST UNUSED MODULES
        # WebEngine aur Multimedia akele 80MB+ hote hain.
        cd ps6_temp/PySide6/Qt/lib/
        rm -rf libQt6WebEngine* libQt6Multimedia* libQt63D* libQt6Charts* libQt6DataVisualization* || true
        cd ../../../..
        
        # 3. REPACK
        rm PySide6-6.8.0-6.8.0-cp311-cp311-android_aarch64.whl
        cd ps6_temp && zip -rq ../PySide6-6.8.0-6.8.0-cp311-cp311-android_aarch64.whl . && cd ..
        rm -rf ps6_temp

    - name: Deploy APK (PySide6 to Android)
      run: |
        # Download Shiboken fresh (No surgery on shiboken to ensure stability)
        wget -q https://download.qt.io/official_releases/QtForPython/shiboken6/shiboken6-6.8.0-6.8.0-cp311-cp311-android_aarch64.whl
        
        pyside6-android-deploy \
          -f \
          --name="QuickCode" \
          --wheel-pyside=PySide6-6.8.0-6.8.0-cp311-cp311-android_aarch64.whl \
          --wheel-shiboken=shiboken6-6.8.0-6.8.0-cp311-cp311-android_aarch64.whl \
          --ndk-path=$ANDROID_NDK_ROOT \
          --sdk-path=$ANDROID_HOME

    - name: Prepare APK for Upload
      if: always()
      run: |
        mkdir -p release
        find . -name "*.apk" -copy {} release/ || find . -name "*.apk" -exec cp {} release/ \;
        ls -lh release/

    - name: Upload APK Artifact
      uses: actions/upload-artifact@v4
      with:
        name: QuickCode-Build
        path: release/*.apk
